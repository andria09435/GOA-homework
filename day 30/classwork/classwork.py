#1) კომენტარის სახით ახსენით რას აკეთებს len(), append(), insert() და pop() ფუნქციები
# len() - აბრუნებს სიის ელემენტების რაოდენობას
# append() - ამატებს ელემენტს სიაში ბოლო პოზიციაზე
# insert() - ამატებს ელემენტს სიაში მითიტებულ პოზიციაზე
# pop() - ამოირებს ელემენტს სიიდან მითითებული პოზიციით (თუ არ არის მითითებული,ამოიღებს ბოლო ელემენტს)